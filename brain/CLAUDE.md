# Brain — Vault Schema (placeholder)

This file is owned by P2 (Vault Architect), which defines the four vault
operations as skills: `/capture`, `/sync`, `/lint`, `/digest`, plus page
templates in `wiki/_templates/`. Until P2 lands, the only rules are the
inherited root constitution plus:

- New material goes in `raw/` untouched.
- Do not hand-edit `index.md` or `log.md` outside a vault operation.

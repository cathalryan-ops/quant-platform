//! Real-stakes execution engine. Built by P8 — P0 scaffolds only.
//!
//! Startup invariants (architecture.md §6 P8): refuse unless approved
//! promotion records exist, guardrails.toml parses, and no root KILL file
//! exists. Dry-run (Alpaca paper endpoints) is the default mode.

fn main() {
    unimplemented!("P8: live engine");
}
